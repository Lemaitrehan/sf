{% extends 'base.html.twig' %}

{% block body %}

    {{ include('header.html.twig') }}

    <div id="content">
        <!--breadcrumbs-->
        <div id="content-header">
            <div id="breadcrumb"><a href="{{ path('admin_index') }}" title="Go to Home" class="tip-bottom"><i class="icon-home"></i>
                    首页</a></div>
        </div>
        <!--End-breadcrumbs-->

        <!--Action boxes-->
        <div class="container-fluid">
            <!--Chart-box-->
            <div class="row-fluid">
                <div class="widget-box">
                    <div class="widget-title"> <span class="icon"> <i class="icon-th"></i> </span>
                        <h5>快递公司管理</h5>
                        <a class="label label-info" href="{{ path('admin_logistics_create') }}">添加</a> </div>
                    <div class="widget-content nopadding">
                        <table class="table table-bordered table-striped">
                            <thead>
                            <tr>
                                <th>#</th>
                                <th>物流名称</th>
                                <th>其否启用</th>
                                <th>操作</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for item in logistics %}
                                <tr>
                                    <td>{{ item.id }}</td>
                                    <td>{{ item.name }}</td>
                                    <td>{% if item.isActive == 1%} 是 {% else %} 否 {% endif %}</td>
                                    <td>
                                        <a href="{{ path('admin_logistics_create', {'id': item.id}) }}" class="btn btn-mini btn-info">编辑</a>
                                        <a href="#" class="btn btn-mini btn-warning">删除</a>
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            <!--End-Chart-box-->

        </div>
    </div>
{% endblock %}